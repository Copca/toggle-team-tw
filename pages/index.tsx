import { NextPage } from 'next';
import { Layout } from '../components/layouts/Layout';

const Home: NextPage = () => {
	return (
		<Layout>
			<div>Home</div>
		</Layout>
	);
};

export default Home;
